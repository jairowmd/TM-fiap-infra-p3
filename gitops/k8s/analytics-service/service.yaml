apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: togglemaster
spec:
  selector:
    app: analytics-service
  type: ClusterIP
  ports:
    - port: 8005
      targetPort: 8005
      protocol: TCP